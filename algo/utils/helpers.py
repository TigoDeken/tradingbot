"""
utils/helpers.py
Shared utility functions used across the project.

Responsibilities:
- round_to_step(value, step): round a quantity down to the nearest qty_step
- load_json(path): load and validate a JSON config file
- utc_now(): return current UTC timestamp as pandas Timestamp
- timeframe_to_minutes(tf): convert timeframe string ("5", "60") to integer minutes
- safe_divide(a, b, default): division with zero-denominator guard
"""

# TODO: implement helper functions
